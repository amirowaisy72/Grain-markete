//Apply all expenses automatically
  const applyExpensesAuto = () => {
    try {
      const calculations = calculateExpenses(
        expenses,
        (crop = crop === 'Sarson' || crop === 'Mirch' || crop === 'Moonji' ? 'Others' : crop),
        (quantity / 40) * rate,
        completeBags,
        incompleteBags,
        member,
        quantity,
        'Buyer',
      )
      setCalculatedExpenses(calculations)
    } catch (error) {
      console.log('no')
    }
  }

when i format document, it becomes like this, how to fix this?